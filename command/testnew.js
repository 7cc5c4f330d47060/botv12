// const index=require("../../index.js");
const settings = require('../settings.json')
module.exports = {
  command: function (c) {
    
  },
  desc: 'This command will not run, it only exists to show the testnew command in the new system.',
  usage: ' [text]'
}
